# android-javamail-api-example
This repo shows an example android application that fetches emails from gmail using javamail api.

Copy and add jar files from ```app/libs/``` as library in your project.
